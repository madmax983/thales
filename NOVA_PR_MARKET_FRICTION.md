🌟 Nova: Market Friction

💡 **The Spark:** "Volume gives magnitude, but what about the effort required for that movement? What if we analyze the friction of price action by comparing volume to price range?"
🚀 **The Feature:** "Implemented `analyze_friction` in `crates/cli/src/experimental/market_friction.rs` and added `analyze-friction` command."
🔮 **The Potential:** "Could identify accumulation/distribution phases when friction is high, or free-fall/melt-up regimes when friction is unusually low, warning of impending exhaustion."
⚠️ **Risk:** "Low. Isolated in `src/experimental/market_friction.rs` and behind the `nova` feature flag."
