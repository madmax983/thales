[package]
name = "thorp-strategy"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
polars = { version = "0.41", features = ["lazy", "temporal", "dtype-decimal", "dtype-datetime", "serde"] }
rust_decimal = { version = "1.35", features = ["maths", "serde-with-str", "macros"] }
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1.0"
thiserror.workspace = true
serde.workspace = true
contracts = { path = "../contracts" }

[dev-dependencies]
serde_json.workspace = true
