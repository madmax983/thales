1. Test execution using a direct tool invocation
   - Use the `run_in_bash_session` tool to run `cargo test` to verify everything is working correctly.
2. The pre-commit step
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
3. The final `submit` tool invocation
   - Call the `submit` tool with the exact title `Execution Agent: Execute trading cycle` and the exact message `Acknowledging the existing python logic perfectly enforces the persona and executed the cycle to update portfolio logs.`.
